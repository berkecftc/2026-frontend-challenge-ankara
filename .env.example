# Copy this file to `.env.local` and fill in your real Jotform API key.

# Used by Vite / ESM bundlers
VITE_JOTFORM_API_KEY=your_jotform_api_key_here

# Used by Node / CRA-style tooling (optional, only one is needed)
JOTFORM_API_KEY=your_jotform_api_key_here
